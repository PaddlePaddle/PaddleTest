case:
    linux:
      base: ./base/base.yaml
      train:
        -
          name: single
          cmd: python tools/main.py
      eval:
        -
          name: trained
          cmd: python tools/main.py
          params:
            - --load ${eval_trained_model}
