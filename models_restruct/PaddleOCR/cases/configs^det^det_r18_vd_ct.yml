case:
    linux:
        base: ./base/ocr_det_base_pretrained.yaml
        train:
          -
            name: multi
            result:
              loss:
                base: 0.285547
                threshold: 0.1
                evaluation: "-"
        eval:
          -
            name: pretrained
            result:
              f_score:
                base: ${eval_base_acc}
                threshold: 0
                evaluation: "="
