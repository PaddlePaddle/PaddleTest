case:
    linux:
        base: ./base/ocr_det_base_pretrained.yaml
        predict:
          -
            name: pretrained
            result:
              f_score:
                base: ${eval_base_acc}
                threshold: 0
                evaluation: "="
